{
    "authenticationMethod": "route",
    "routes":[
        {
            "source": "^/sales-order/(.*)",
            "destination": "srv-api",
            "csrProtection": true,
            "authenticationType": "xsuaa"
        }
    ]
}